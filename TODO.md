# TODO

- [ ] Add a scrolling animation in the per-year view, and update it to only show the current year by default.
- [ ] Make the app's theme update dynamically when the device wallpaper changes while the app is running.
